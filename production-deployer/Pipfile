[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = ">=3.0.3, ==3.*"
watchdog = ">=5.0.3, ==5.*"
crossplane = ">=0.5.8, ==0.5.*"
pydantic = ">=2.9.2, ==2.*"

[dev-packages]
ruff = ">=0.6.9, ==0.6.*"

[requires]
python_version = "3.12"
